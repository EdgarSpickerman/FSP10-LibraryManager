table
  thead
    tr
      th Title
      th Author
      th Genre
      th Year Released
  tbody
    if(books)
      each book in books
        tr
          td
            a(href=`/details/books/#{book.id}`) #{book.title}
          td #{book.author}
          td #{book.genre}
          td #{book.first_published}
